<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper>
<!--
    UserMapper映射文件示例
    
    namespace: 对应的Mapper接口全限定名
    每个SQL标签对应接口中的一个方法
-->
<mapper namespace="com.mybatis.test.mapper.UserMapper">
    
    <!-- 
        查询：根据ID查询用户
        id: 方法名
        parameterType: 参数类型
        resultType: 返回值类型
    -->
    <select id="selectById" parameterType="java.lang.Long" resultType="com.mybatis.test.entity.User">
        SELECT id, username, password, email, create_time
        FROM user
        WHERE id = #{id}
    </select>
    
    <!-- 查询：查询所有用户 -->
    <select id="selectAll" resultType="com.mybatis.test.entity.User">
        SELECT id, username, password, email, create_time
        FROM user
        ORDER BY id
    </select>
    
    <!-- 插入：添加用户 -->
    <insert id="insert" parameterType="com.mybatis.test.entity.User">
        INSERT INTO user (username, password, email, create_time)
        VALUES (#{username}, #{password}, #{email}, #{createTime})
    </insert>
    
    <!-- 更新：更新用户信息 -->
    <update id="update" parameterType="com.mybatis.test.entity.User">
        UPDATE user
        SET username = #{username},
            password = #{password},
            email = #{email}
        WHERE id = #{id}
    </update>
    
    <!-- 删除：根据ID删除用户 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM user WHERE id = #{id}
    </delete>
    
</mapper>

