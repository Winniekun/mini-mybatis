# MyBatis源码对比分析

## 📊 项目对比

| 功能模块 | Mini-MyBatis | 真实MyBatis | 相似度 |
|---------|-------------|------------|-------|
| 配置解析 | 简化版XML解析 | 完整的XML解析+注解 | ⭐⭐⭐ |
| SqlSession | 核心接口实现 | 完整实现 | ⭐⭐⭐⭐⭐ |
| Executor | SimpleExecutor | Simple/Reuse/Batch三种 | ⭐⭐⭐⭐ |
| StatementHandler | 基础实现 | Prepared/Simple/Callable | ⭐⭐⭐ |
| ResultSetHandler | 自动映射 | 自动+手动+嵌套映射 | ⭐⭐⭐ |
| Mapper代理 | 核心实现 | 完整实现 | ⭐⭐⭐⭐⭐ |
| 插件机制 | 责任链+代理 | 完整实现 | ⭐⭐⭐⭐ |
| 缓存 | 一级缓存基础 | 一级+二级+多种策略 | ⭐⭐⭐ |
| 动态SQL | 未实现 | 完整的OGNL表达式 | ⭐ |
| 事务管理 | 简单实现 | JDBC/MANAGED/Spring | ⭐⭐⭐ |

## 🔍 核心类对比

### 1. SqlSessionFactory

**Mini-MyBatis**:
```java
public interface SqlSessionFactory {
    SqlSession openSession();
    SqlSession openSession(boolean autoCommit);
    Configuration getConfiguration();
}
```

**真实MyBatis**:
```java
public interface SqlSessionFactory {
    SqlSession openSession();
    SqlSession openSession(boolean autoCommit);
    SqlSession openSession(Connection connection);
    SqlSession openSession(TransactionIsolationLevel level);
    SqlSession openSession(ExecutorType execType);
    SqlSession openSession(ExecutorType execType, boolean autoCommit);
    // ... 更多重载方法
    Configuration getConfiguration();
}
```

**差异**:
- 真实MyBatis支持更多的SqlSession创建方式
- 支持指定事务隔离级别
- 支持指定执行器类型
- 支持传入外部连接

---

### 2. Executor

**Mini-MyBatis**:
```java
public interface Executor {
    <E> List<E> query(String statementId, Object parameter);
    int update(String statementId, Object parameter);
    void commit();
    void rollback();
    void close();
}
```

**真实MyBatis**:
```java
public interface Executor {
    ResultHandler NO_RESULT_HANDLER = null;
    
    int update(MappedStatement ms, Object parameter);
    
    <E> List<E> query(MappedStatement ms, Object parameter, 
                      RowBounds rowBounds, ResultHandler resultHandler);
    
    <E> List<E> query(MappedStatement ms, Object parameter, 
                      RowBounds rowBounds, ResultHandler resultHandler, 
                      CacheKey cacheKey, BoundSql boundSql);
    
    <E> Cursor<E> queryCursor(MappedStatement ms, Object parameter, 
                               RowBounds rowBounds);
    
    List<BatchResult> flushStatements();
    
    void commit(boolean required);
    void rollback(boolean required);
    
    CacheKey createCacheKey(MappedStatement ms, Object parameterObject, 
                            RowBounds rowBounds, BoundSql boundSql);
    
    boolean isCached(MappedStatement ms, CacheKey key);
    void clearLocalCache();
    void deferLoad(MappedStatement ms, MetaObject resultObject, 
                   String property, CacheKey key, Class<?> targetType);
    
    Transaction getTransaction();
    void close(boolean forceRollback);
    boolean isClosed();
    void setExecutorWrapper(Executor executor);
}
```

**差异**:
- 真实MyBatis的Executor接收MappedStatement而不是statementId
- 支持RowBounds分页
- 支持ResultHandler自定义结果处理
- 支持游标查询（Cursor）
- 支持批量操作
- 支持延迟加载
- 更完善的事务控制

---

### 3. MapperProxy

**核心原理一致！** ⭐⭐⭐⭐⭐

Mini-MyBatis的实现已经抓住了核心：
1. JDK动态代理
2. InvocationHandler拦截方法调用
3. 根据方法签名找到SQL
4. 通过SqlSession执行

真实MyBatis的增强：
- 支持方法注解（@Select、@Insert等）
- 支持参数注解（@Param）
- 更智能的方法类型判断
- 支持返回值的类型转换

---

## 📖 源码阅读路线

### 入门路线（3天）

**Day 1: 初始化流程**
1. `SqlSessionFactoryBuilder.build()`
2. `XMLConfigBuilder.parse()`
3. `XMLMapperBuilder.parse()`
4. `Configuration` 的构建过程

**关键点**：
- 配置文件如何解析
- MappedStatement如何创建
- Mapper接口如何注册

---

**Day 2: SQL执行流程**
1. `MapperProxy.invoke()`
2. `MapperMethod.execute()`
3. `DefaultSqlSession.selectOne()`
4. `SimpleExecutor.query()`
5. `PreparedStatementHandler.query()`
6. `DefaultResultSetHandler.handleResultSets()`

**关键点**：
- 方法调用如何转换为SQL执行
- 参数如何设置
- 结果如何映射

---

**Day 3: 高级特性**
1. 插件机制：`InterceptorChain.pluginAll()`
2. 缓存机制：`PerpetualCache`、`CachingExecutor`
3. 动态SQL：`DynamicSqlSource`、`SqlNode`

**关键点**：
- 插件如何拦截四大对象
- 缓存的实现和失效机制
- 动态SQL的解析过程

---

### 进阶路线（7天）

1. **TypeHandler体系** - 类型处理
2. **ResultMap解析** - 复杂结果映射
3. **Association/Collection** - 关联查询
4. **延迟加载** - 懒加载实现
5. **批处理** - BatchExecutor
6. **Spring集成** - SqlSessionTemplate
7. **MyBatis-Plus** - 增强功能

---

## 💡 面试题精选

### 基础篇

**1. MyBatis是什么？解决了什么问题？**

**答**：
MyBatis是一个优秀的持久层框架，支持定制化SQL、存储过程和高级映射。

解决的问题：
- JDBC代码繁琐：Connection、Statement、ResultSet的管理
- SQL硬编码：SQL语句写在Java代码中，不易维护
- 参数设置麻烦：需要手动setObject
- 结果集处理繁琐：需要手动从ResultSet中取数据

---

**2. MyBatis的执行流程？**

**答**：
```
初始化阶段：
SqlSessionFactoryBuilder 
→ 解析配置文件
→ 创建Configuration
→ 生成SqlSessionFactory

执行阶段：
SqlSessionFactory.openSession()
→ 创建SqlSession
→ getMapper()获取Mapper代理对象
→ 调用Mapper方法
→ MapperProxy拦截
→ 执行Executor
→ 使用StatementHandler执行SQL
→ ResultSetHandler处理结果
→ 返回数据
```

---

**3. #{}和${}的区别？**

**答**：

| 特性 | #{} | ${} |
|-----|-----|-----|
| 处理方式 | 预编译 | 字符串替换 |
| SQL类型 | PreparedStatement | Statement |
| SQL注入 | 安全 | 不安全 |
| 性能 | 高（可缓存） | 低 |
| 使用场景 | 参数传递 | 动态表名、列名 |

示例：
```sql
-- #{}：生成 WHERE id = ?
SELECT * FROM user WHERE id = #{id}

-- ${}：生成 WHERE id = 1（直接拼接）
SELECT * FROM user WHERE id = ${id}
```

---

### 核心篇

**4. Mapper接口如何工作的？**

**答**：
通过JDK动态代理实现，核心流程：

1. **注册阶段**：解析Mapper.xml时，将namespace对应的接口注册到MapperRegistry
2. **获取代理**：调用getMapper()时，MapperProxyFactory创建代理对象
3. **方法拦截**：MapperProxy实现InvocationHandler，拦截方法调用
4. **执行SQL**：根据"namespace.方法名"找到MappedStatement，通过SqlSession执行
5. **返回结果**：根据返回值类型选择selectOne/selectList等

代码示例：
```java
// 1. 用户代码
UserMapper mapper = sqlSession.getMapper(UserMapper.class);
User user = mapper.selectById(1L);

// 2. 底层实现
Proxy.newProxyInstance(
    classLoader, 
    new Class[]{UserMapper.class}, 
    new MapperProxy(sqlSession, UserMapper.class)
);
```

---

**5. MyBatis的插件原理？**

**答**：
基于责任链模式+动态代理实现。

**可拦截的四大对象**：
- Executor：SQL执行器
- StatementHandler：语句处理器
- ParameterHandler：参数处理器
- ResultSetHandler：结果处理器

**工作流程**：
```java
// 1. 配置插件
@Intercepts({
    @Signature(type = Executor.class, method = "query", 
               args = {MappedStatement.class, Object.class, 
                       RowBounds.class, ResultHandler.class})
})
public class MyPlugin implements Interceptor {
    public Object intercept(Invocation invocation) {
        // 前置处理
        Object result = invocation.proceed();
        // 后置处理
        return result;
    }
}

// 2. 创建四大对象时，会经过插件链包装
Executor executor = new SimpleExecutor();
executor = (Executor) interceptorChain.pluginAll(executor);
```

**常见应用**：
- 分页插件：拦截query方法，添加LIMIT
- SQL日志：拦截StatementHandler，打印完整SQL
- 性能监控：拦截执行，记录耗时
- 数据权限：拦截SQL，添加WHERE条件

---

**6. MyBatis的一级缓存和二级缓存？**

**答**：

**一级缓存（Local Cache）**：
- 级别：SqlSession级别
- 开启：默认开启，不能关闭
- 生命周期：SqlSession创建到关闭
- 失效：commit、rollback、close、update操作
- 作用：减少同一个SqlSession的重复查询

**二级缓存（Second Level Cache）**：
- 级别：Mapper级别（namespace）
- 开启：需要配置`<cache/>`标签
- 生命周期：应用级别
- 失效：执行update操作时清空
- 线程安全：需要实体类实现Serializable

**缓存查询顺序**：
```
二级缓存 → 一级缓存 → 数据库
```

**注意事项**：
- 一级缓存可能导致脏读（多个SqlSession）
- 二级缓存在分布式环境下有问题
- 推荐使用Redis等专业缓存

---

### 高级篇

**7. MyBatis的三种Executor的区别？**

**答**：

| 执行器 | 特点 | 适用场景 |
|-------|------|---------|
| SimpleExecutor | 每次执行都创建新Statement | 默认，普通查询 |
| ReuseExecutor | 复用Statement | 需要多次执行相同SQL |
| BatchExecutor | 批量执行 | 批量插入/更新 |

示例：
```java
// SimpleExecutor
stmt = conn.prepareStatement(sql);  // 每次都创建
stmt.execute();
stmt.close();

// ReuseExecutor
if (stmtMap.containsKey(sql)) {
    stmt = stmtMap.get(sql);  // 复用
} else {
    stmt = conn.prepareStatement(sql);
    stmtMap.put(sql, stmt);
}

// BatchExecutor
stmt = conn.prepareStatement(sql);
stmt.addBatch();  // 添加到批次
// ... 添加多次
stmt.executeBatch();  // 批量执行
```

---

**8. MyBatis如何处理延迟加载？**

**答**：
通过CGLib或Javassist动态代理实现。

**原理**：
1. 查询主对象时，关联对象设置为代理对象
2. 当访问关联对象的属性时，触发代理
3. 代理对象执行关联查询
4. 返回真实对象

**配置**：
```xml
<settings>
    <!-- 开启延迟加载 -->
    <setting name="lazyLoadingEnabled" value="true"/>
    <!-- 关闭积极加载 -->
    <setting name="aggressiveLazyLoading" value="false"/>
</settings>
```

**注意**：
- 延迟加载可能导致N+1问题
- 需要在SqlSession关闭前访问
- 可能触发额外的SQL

---

**9. MyBatis的动态SQL原理？**

**答**：
基于OGNL表达式和SqlNode树实现。

**核心类**：
- `SqlNode`：SQL节点接口
- `IfSqlNode`：处理`<if>`标签
- `ForeachSqlNode`：处理`<foreach>`标签
- `TextSqlNode`：处理文本
- `MixedSqlNode`：组合多个节点

**执行流程**：
```
解析阶段：
XML标签 → SqlNode树

执行阶段：
遍历SqlNode树 → OGNL表达式求值 → 拼接SQL
```

示例：
```xml
<select id="selectUsers" parameterType="map" resultType="User">
    SELECT * FROM user
    WHERE 1=1
    <if test="name != null">
        AND name = #{name}
    </if>
    <if test="age != null">
        AND age = #{age}
    </if>
</select>
```

---

**10. MyBatis与Hibernate的区别？**

**答**：

| 特性 | MyBatis | Hibernate |
|-----|---------|-----------|
| 类型 | 半自动ORM | 全自动ORM |
| SQL控制 | 完全控制 | HQL，不直接写SQL |
| 学习曲线 | 平缓 | 陡峭 |
| 灵活性 | 高 | 低 |
| 开发效率 | 中等 | 高（单表CRUD） |
| 性能优化 | 容易 | 较难 |
| 缓存 | 简单 | 复杂 |
| 适用场景 | 复杂SQL、需要优化 | 标准CRUD、快速开发 |

**选择建议**：
- 需要精细控制SQL → MyBatis
- 快速开发标准CRUD → Hibernate/JPA
- 复杂查询、性能要求高 → MyBatis
- 跨数据库、标准化 → Hibernate

---

## 🎯 面试准备建议

### 1. 理解核心流程
- 能画出MyBatis的完整执行流程图
- 能说出每个核心类的作用
- 理解各个组件之间的关系

### 2. 掌握关键技术
- JDK动态代理的原理
- 反射的应用
- JDBC的使用
- XML解析
- 设计模式的应用

### 3. 实践经验
- 写过完整的MyBatis应用
- 遇到过问题并解决
- 优化过SQL性能
- 使用过插件

### 4. 对比学习
- 与JDBC的区别
- 与Hibernate的区别
- 与MyBatis-Plus的关系

### 5. 深入源码
- 读过核心类的源码
- 理解关键方法的实现
- 知道为什么这样设计

---

## 📚 推荐资源

### 官方资源
- [MyBatis官方文档](https://mybatis.org/mybatis-3/zh/index.html)
- [MyBatis GitHub](https://github.com/mybatis/mybatis-3)

### 书籍
- 《MyBatis技术内幕》
- 《MyBatis从入门到精通》

### 博客
- 美团技术博客
- 掘金MyBatis专栏

### 视频
- B站尚硅谷MyBatis教程
- 黑马程序员MyBatis课程

---

## ✅ 学习检查清单

- [ ] 能说出MyBatis的完整执行流程
- [ ] 理解Mapper接口的工作原理
- [ ] 能解释#{}和${}的区别
- [ ] 理解一级缓存和二级缓存
- [ ] 知道插件的工作原理
- [ ] 理解三种Executor的区别
- [ ] 能分析动态SQL的实现
- [ ] 读过核心类的源码
- [ ] 画过时序图和类图
- [ ] 能对比MyBatis和Hibernate

---

**🎉 恭喜你完成了MyBatis的深度学习！继续加油！**

