# 📍 Day 4 学习地图 - ResultSetHandler全景图

## 🗺️ ResultSetHandler在架构中的位置

```
                    MyBatis完整架构
                           │
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
   SqlSession         Configuration        Mapper
        │
        ↓
   ┌─────────┐
   │Executor │
   └────┬────┘
        ↓
   ┌──────────────────┐
   │StatementHandler  │
   │  - prepare()     │
   │  - parameterize()│
   │  - query()       │
   └────┬─────────────┘
        ↓
   ┌──────────────────────────────┐
   │   ResultSetHandler ⭐         │ ← Day 4 的主角！
   │   - handleResultSet()        │
   │   - createResultObject()     │
   │   - handleBeanType()         │
   │   - convertType()            │
   │   - camelToUnderscore()      │
   └──────────────────────────────┘
```

---

## 🔄 ResultSetHandler执行流程图

```
StatementHandler.query() 执行完毕
        ↓
   获得 ResultSet
        ↓
┌────────────────────────────────────────┐
│ ResultSetHandler.handleResultSet()    │
└─────────────┬──────────────────────────┘
              ↓
       ┌──────────────┐
       │ 获取元数据   │
       └──────┬───────┘
              ↓
    ResultSetMetaData metaData
    columnCount = 3
    columns = ["id", "user_name", "email"]
              ↓
       ┌──────────────┐
       │ 开始遍历     │
       │while(rs.next())│
       └──────┬───────┘
              ↓
    ┌─────────────────────────┐
    │ 第1行数据               │
    │ id=1, user_name="admin"│
    └──────────┬──────────────┘
               ↓
    ┌──────────────────────┐
    │ createResultObject() │
    └──────────┬───────────┘
               ↓
         ┌────────────┐
         │ 创建对象   │
         │User user = │
         │  new User()│
         └─────┬──────┘
               ↓
         ┌─────────────────┐
         │ 建立字段映射表  │
         └─────┬───────────┘
               ↓
    Map<String, Field> fieldMap = {
        "id" → id字段,
        "username" → username字段,
        "user_name" → username字段,  ← 支持驼峰
        "email" → email字段
    }
               ↓
         ┌──────────────┐
         │ 遍历所有列   │
         │for each column│
         └─────┬────────┘
               ↓
    ┌──────────────────────────────┐
    │ 第1列：id                    │
    │ columnName = "id"            │
    │ columnValue = 1L             │
    └──────────┬───────────────────┘
               ↓
         ┌──────────────┐
         │ 查找字段     │
         └─────┬────────┘
               ↓
    field = fieldMap.get("id")
         ↓ 找到了！
         ┌──────────────┐
         │ 类型转换     │
         └─────┬────────┘
               ↓
    value = convertType(1L, Long.class)
         ↓ 类型相同，直接返回
         ┌──────────────┐
         │ 设置值       │
         └─────┬────────┘
               ↓
    field.set(user, 1L)
    // user.id = 1L ✅
               ↓
    ┌──────────────────────────────┐
    │ 第2列：user_name             │
    │ columnName = "user_name"     │
    │ columnValue = "admin"        │
    └──────────┬───────────────────┘
               ↓
    field = fieldMap.get("user_name")
         ↓ 找到了！(驼峰转换)
    value = convertType("admin", String.class)
         ↓
    field.set(user, "admin")
    // user.username = "admin" ✅
               ↓
    ┌──────────────────────────────┐
    │ 第3列：email                 │
    │ columnName = "email"         │
    │ columnValue = "a@xx.com"     │
    └──────────┬───────────────────┘
               ↓
    field.set(user, "a@xx.com")
    // user.email = "a@xx.com" ✅
               ↓
         ┌──────────────┐
         │ 添加到列表   │
         └─────┬────────┘
               ↓
    resultList.add(user)
               ↓
    ┌─────────────────────────┐
    │ 第2行数据...            │
    └──────────┬──────────────┘
               ↓
         (重复上述过程)
               ↓
    ┌──────────────────────┐
    │ 没有更多数据         │
    │ rs.next() = false    │
    └──────────┬───────────┘
               ↓
         ┌──────────────┐
         │ 返回结果     │
         └─────┬────────┘
               ↓
    return resultList
    // List<User> [user1, user2, ...]
```

---

## 🎨 驼峰命名转换示意图

```
┌────────────────────────────────────────────┐
│        Java驼峰命名（camelCase）            │
│              userName                       │
└────────────────┬───────────────────────────┘
                 ↓ 转换算法
         ┌───────────────┐
         │  遍历每个字符  │
         └───────┬───────┘
                 ↓
    ┌────────────────────────────┐
    │ u → 'u' (第一个字符小写)    │
    │ s → 's'                    │
    │ e → 'e'                    │
    │ r → 'r'                    │
    │ N → '_n' (大写加下划线)     │
    │ a → 'a'                    │
    │ m → 'm'                    │
    │ e → 'e'                    │
    └────────────┬───────────────┘
                 ↓
┌────────────────────────────────────────────┐
│      数据库下划线命名（snake_case）         │
│              user_name                      │
└────────────────────────────────────────────┘

更多示例：
createTime    →  create_time
isActive      →  is_active
userId        →  user_id
userDetailInfo → user_detail_info
```

---

## 🔧 字段映射表构建图

```
Java类: User.class
    ↓ 反射获取字段
┌──────────────────────────┐
│ Field[] fields = {       │
│   id,                    │
│   username,              │
│   email,                 │
│   createTime             │
│ }                        │
└──────────┬───────────────┘
           ↓ 遍历处理
    ┌──────────────┐
    │ field.setAccessible(true)  ← 允许访问private
    └──────┬───────┘
           ↓
    ┌─────────────────────────────┐
    │ 策略1: 原始名称(小写)        │
    │ fieldMap.put("id", id字段)  │
    │ fieldMap.put("username",    │
    │              username字段)  │
    │ fieldMap.put("email",       │
    │              email字段)     │
    │ fieldMap.put("createtime",  │
    │              createTime字段)│
    └──────────┬──────────────────┘
               ↓
    ┌─────────────────────────────┐
    │ 策略2: 下划线名称(小写)      │
    │ fieldMap.put("id", id字段)  │
    │ fieldMap.put("user_name",   │
    │              username字段)  │
    │ fieldMap.put("email",       │
    │              email字段)     │
    │ fieldMap.put("create_time", │
    │              createTime字段)│
    └──────────┬──────────────────┘
               ↓
┌────────────────────────────────────┐
│    最终的字段映射表                 │
│ Map<String, Field> fieldMap = {    │
│   "id" → id字段,                   │
│   "username" → username字段,       │
│   "user_name" → username字段, ⭐   │
│   "email" → email字段,             │
│   "createtime" → createTime字段,   │
│   "create_time" → createTime字段 ⭐│
│ }                                  │
│                                    │
│ 这样无论数据库字段是：              │
│ - username 还是 user_name          │
│ - createtime 还是 create_time      │
│ 都能找到对应的Java属性！           │
└────────────────────────────────────┘
```

---

## 🔄 类型转换流程图

```
数据库返回值
    ↓
columnValue (Object类型)
    ↓
┌─────────────────────────┐
│ convertType()           │
│ (columnValue, targetType)│
└──────────┬──────────────┘
           ↓
    ┌──────────────┐
    │ 判断: 是否null? │
    └──────┬─────────┘
           ↓ 不是null
    ┌──────────────────────┐
    │ 判断: 类型是否相同?   │
    └──────┬───────────────┘
           ↓ 不相同
    ┌──────────────────────┐
    │ 判断: 目标是String?   │
    └──────┬───────────────┘
           ↓ 不是
    ┌──────────────────────┐
    │ 判断: 目标是Integer?  │
    └──────┬───────────────┘
           ↓ 是
    ┌──────────────────────┐
    │ 判断: 源是Number?     │
    └──────┬───────────────┘
      ↓ 是        ↓ 不是
  intValue()  parseInt()
      ↓            ↓
    返回Integer值
           ↓
    设置到字段

类型转换矩阵:
┌──────────┬───────────┬──────────────┐
│ 源类型   │ 目标类型  │ 转换方式     │
├──────────┼───────────┼──────────────┤
│ Long     │ int       │ .intValue()  │
│ Integer  │ Long      │ .longValue() │
│ String   │ Integer   │ parseInt()   │
│ Integer  │ String    │ toString()   │
│ Integer  │ Boolean   │ != 0         │
│ String   │ Boolean   │ parseBoolean│
└──────────┴───────────┴──────────────┘
```

---

## 📊 三种结果类型处理图

```
                createResultObject()
                        ↓
                ┌───────────────┐
                │ 判断resultType│
                └───────┬───────┘
                        ↓
        ┌───────────────┼───────────────┐
        ↓               ↓               ↓
   ┌─────────┐    ┌─────────┐    ┌──────────┐
   │简单类型 │    │Map类型  │    │JavaBean  │
   └────┬────┘    └────┬────┘    └─────┬────┘
        ↓              ↓                ↓

┌────────────────┐  ┌──────────────┐  ┌─────────────────┐
│ String         │  │ Map          │  │ User            │
│ Integer        │  │ HashMap      │  │ Product         │
│ Long           │  │ LinkedHashMap│  │ Order           │
│ Double         │  │ ...          │  │ 自定义Bean      │
└────┬───────────┘  └──────┬───────┘  └─────┬───────────┘
     ↓                     ↓                ↓

┌────────────────┐  ┌──────────────┐  ┌─────────────────┐
│ 直接返回第1列  │  │ 每列作为entry│  │ 自动映射属性    │
│                │  │              │  │                 │
│ SELECT COUNT(*)│  │ map.put(     │  │ 1. 创建对象     │
│ FROM user      │  │   "id", 1L   │  │ 2. 建立映射表   │
│ ↓              │  │ );           │  │ 3. 遍历列       │
│ return rs.     │  │ map.put(     │  │ 4. 查找字段     │
│   getObject(1) │  │   "name",    │  │ 5. 类型转换     │
│                │  │   "admin"    │  │ 6. 设置值       │
│ 返回: 5L       │  │ );           │  │                 │
│                │  │ return map;  │  │ 返回: User对象  │
└────────────────┘  └──────────────┘  └─────────────────┘
```

---

## 🎯 完整查询流程（四大对象协作）

```
┌─────────────────────────────────────────────┐
│           用户代码                           │
│  List<User> users = mapper.selectAll();    │
└────────────────┬────────────────────────────┘
                 ↓
┌─────────────────────────────────────────────┐
│         MapperProxy (动态代理)               │
│  - 拦截方法调用                              │
│  - 构建statementId                          │
│  - 委托给SqlSession                         │
└────────────────┬────────────────────────────┘
                 ↓
┌─────────────────────────────────────────────┐
│           SqlSession (门面)                  │
│  - selectList(statementId, parameter)      │
│  - 委托给Executor                           │
└────────────────┬────────────────────────────┘
                 ↓
┌─────────────────────────────────────────────┐
│          Executor (执行器)                   │
│  Day 3学习内容 ↓                            │
│  ┌─────────────────────────────────────┐   │
│  │ 1. 获取MappedStatement              │   │
│  │ 2. 创建StatementHandler             │   │
│  │ 3. 协调执行流程                     │   │
│  └─────────────────────────────────────┘   │
└────────────────┬────────────────────────────┘
                 ↓
┌─────────────────────────────────────────────┐
│      StatementHandler (JDBC封装)            │
│  Day 3学习内容 ↓                            │
│  ┌─────────────────────────────────────┐   │
│  │ 1. prepare() - 创建PreparedStatement│   │
│  │    #{} → ?                          │   │
│  │ 2. parameterize() - 设置参数        │   │
│  │    ps.setObject(1, parameter)      │   │
│  │ 3. query() - 执行SQL                │   │
│  │    ResultSet rs = ps.executeQuery()│   │
│  └─────────────────────────────────────┘   │
└────────────────┬────────────────────────────┘
                 ↓
            ResultSet (JDBC结果集)
                 ↓
┌─────────────────────────────────────────────┐
│      ResultSetHandler (结果映射) ⭐          │
│  Day 4学习内容 ↓                            │
│  ┌─────────────────────────────────────┐   │
│  │ 1. handleResultSet() - 入口         │   │
│  │    - 获取元数据                     │   │
│  │    - 遍历结果集                     │   │
│  │                                     │   │
│  │ 2. createResultObject() - 创建对象  │   │
│  │    - 判断结果类型                   │   │
│  │    - 分发处理                       │   │
│  │                                     │   │
│  │ 3. handleBeanType() - Bean映射      │   │
│  │    - 创建对象实例                   │   │
│  │    - 建立字段映射表                 │   │
│  │    - 遍历列设置值                   │   │
│  │                                     │   │
│  │ 4. convertType() - 类型转换         │   │
│  │    - 判断类型是否匹配               │   │
│  │    - 执行必要的转换                 │   │
│  │                                     │   │
│  │ 5. camelToUnderscore() - 命名转换   │   │
│  │    - userName → user_name          │   │
│  └─────────────────────────────────────┘   │
└────────────────┬────────────────────────────┘
                 ↓
          List<User> (结果列表)
                 ↓
            返回给用户
```

---

## 🧩 ResultSet的结构图

```
ResultSet = 一个可移动的游标 + 数据

初始状态 (创建后):
┌──────────────────┐
│   游标位置       │ ← 在第一行之前
├──────────────────┤
│ 第1行: id=1,     │
│    username="a"  │
├──────────────────┤
│ 第2行: id=2,     │
│    username="b"  │
├──────────────────┤
│ 第3行: id=3,     │
│    username="c"  │
└──────────────────┘

调用 rs.next() 后:
┌──────────────────┐
│ 第1行: id=1,     │ ← 游标移到这里 ⭐
│    username="a"  │   可以读取数据
├──────────────────┤
│ 第2行: id=2,     │
│    username="b"  │
├──────────────────┤
│ 第3行: id=3,     │
│    username="c"  │
└──────────────────┘

读取当前行数据:
┌────────────────────────────────┐
│ rs.getLong("id")         → 1L  │
│ rs.getString("username") → "a" │
│ rs.getObject(1)          → 1L  │
│ rs.getObject(2)          → "a" │
└────────────────────────────────┘

继续调用 rs.next():
┌──────────────────┐
│ 第1行: id=1,     │
│    username="a"  │
├──────────────────┤
│ 第2行: id=2,     │ ← 游标移到第2行
│    username="b"  │
├──────────────────┤
│ 第3行: id=3,     │
│    username="c"  │
└──────────────────┘

当没有更多数据时:
rs.next() 返回 false
退出循环
```

---

## 🔍 自动映射的策略图

```
数据库字段: user_name
        ↓
   查找Java属性
        ↓
┌───────────────────────┐
│ 策略1: 直接匹配       │
│ fieldMap.get(         │
│   "user_name"         │
│ )                     │
└──────┬────────────────┘
       ↓ 找到了？
      是 → 返回字段
      否 → 策略2
       ↓
┌───────────────────────┐
│ 策略2: 小写匹配       │
│ fieldMap.get(         │
│   "user_name".        │
│   toLowerCase()       │
│ )                     │
└──────┬────────────────┘
       ↓ 找到了？
      是 → 返回字段
      否 → 策略3
       ↓
┌───────────────────────┐
│ 策略3: 驼峰转换       │
│ "user_name" 对应      │
│ userName 字段吗?      │
│                       │
│ fieldMap已经包含:     │
│ "user_name" → userName│
│ 字段                  │
└──────┬────────────────┘
       ↓ 找到了！
    返回 userName 字段
```

---

## 💡 核心设计模式

### 1. 模板方法模式

```
handleResultSet() {
    获取元数据
    while (遍历结果集) {
        创建对象       ← 子步骤
        映射字段       ← 子步骤
        类型转换       ← 子步骤
        添加到列表
    }
    返回列表
}

每个子步骤都是独立的方法
便于扩展和维护
```

### 2. 策略模式

```
createResultObject() {
    if (简单类型) {
        策略1: 直接返回第一列
    } else if (Map类型) {
        策略2: 转为Map
    } else {
        策略3: 转为JavaBean
    }
}

根据不同的类型选择不同的处理策略
```

### 3. 反射技术

```
不需要生成代码
运行时动态创建对象
运行时动态设置属性
灵活但有性能开销
```

---

## 🎯 学习路径回顾

```
Day 4: ResultSetHandler
       │
       ├─ Part 1: ResultSetHandler详解
       │         ├─ 核心职责
       │         ├─ 核心方法
       │         └─ 执行流程
       │
       ├─ Part 2: 自动映射机制
       │         ├─ 映射策略
       │         ├─ 驼峰转换
       │         └─ 类型转换
       │
       └─ Part 3&4: 复杂映射与实践
                 ├─ 关联查询
                 ├─ 一对一/一对多
                 └─ 完整CRUD
```

---

## 📚 知识点权重

```
重要程度分布:

⭐⭐⭐⭐⭐ 必须掌握:
  - ResultSetHandler的作用
  - 自动映射原理
  - 驼峰命名转换
  - 基本类型转换

⭐⭐⭐⭐ 重要:
  - handleResultSet流程
  - 字段映射表构建
  - 反射技术应用
  - 问题排查方法

⭐⭐⭐ 了解:
  - 复杂映射场景
  - 关联查询处理
  - 性能优化
  - 真实MyBatis特性
```

---

**这份学习地图帮助你建立ResultSetHandler的完整认知！** 📌

